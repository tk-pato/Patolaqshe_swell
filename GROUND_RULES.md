# グランドルール

## ターミナル実行ルール

**絶対厳守: ターミナルコマンド実行時に許可を求めない**

- `run_in_terminal` を使用する際、事前説明や確認は一切不要
- コマンドを直ちに実行し、結果のみ報告すること
- 「実行してもよろしいですか？」等の確認は完全禁止
- ユーザーが指示した場合は即座に実行する

## アップロードフロー

1. ファイル修正
2. `/tmp/rsync_files.txt` 作成（アップロード対象を列挙）
3. `expect scripts/upload.exp` で自動アップロード
4. `expect scripts/verify.exp` でサーバー確認（任意）
5. Git add/commit/push

**全て自動実行、許可不要**
