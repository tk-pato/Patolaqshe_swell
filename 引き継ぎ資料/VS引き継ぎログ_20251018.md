# VS Code引き継ぎログ - 2025年10月18日

## 2025年11月5日 追記: Footer Asti完全再現プロンプトの評価

- `footer_asti_100percent.md` の実行は見送り。以下の理由で再度崩壊リスクが高いと判断。
   - Step3 の置換で `is_front_page()` ブロックが削除され、全ページでカスタムフッターが出力される。過去の「3重表示」問題を再発させる危険が高い。
   - CSS は `footer.css` のみを想定しているが、Google Fonts の読み込みや `footer-sp.css` の扱いが未整理。100%再現は不可能、表示崩れの懸念あり。
   - HTML を全面置換する方針は VS の「footer.php は極力触らない」指針と矛盾。ヘッダーバー破損を再発させる恐れがある。
- 以上より、まずは CSS のみで既存構造に合わせるプランを優先し、`footer_asti_100percent.md` は実行しない。

## 2025年11月5日 追記: Astiフッターデザイン正確トレース実装（安全版）の評価

- `asti-footer-safe-implementation.md` は現状適用不可。Step1 が `patolaqshe_enqueue_styles()` を全置換し、既存の CSS 読み込みロジック（Sectionごとの依存やPC/SP読み分け）を丸ごと消してしまう設計。サイト全体のスタイルが崩壊する。
- Step2 は `footer.css` をトップページ限定用に書き直すが、Step1 で `child_style` などの依存が途切れるため整合性が取れない。キャッシュバスターも消失する。
- Step3 の HTML 置換は `is_front_page()` を維持しているが、既存の `.ptl-footer*` 構造を破棄するため、過去作業の再利用が不能になる。現行の VS ガイドライン（HTMLは可能な限り触らず CSS で合わせる）とも矛盾。
- Step4 で `footer-sp.css` を空ファイル化すると、PC/SP 分離方針（Phase②以降）に逆行し、他セクションとの統一管理が崩れる。
- よって、このプロンプトは実行しない。CSSのみで既存構造に追従させる新プランを再設計する。

## 2025年11月5日 追記: Astiフッターデザイン正確トレース実装（既存構造維持版）の評価

- `asti-footer-css-only-implementation.md` は CSS だけで解決するとしながら、`body #footer` や `body #footer ul` など親テーマ共通のセレクタを上書きするため、トップページ以外の標準フッター表示まで巻き込んで崩壊する危険が高い。
- Google Fonts を各CSSファイルの `@import` で読み込む構成は、既存のキャッシュバスター／依存関係と矛盾し、パフォーマンス低下や二重ロードの懸念がある。
- `font-family: 'Noto+Sans JP'` のようにフォント名が誤記されており、提示どおり実装しても意図したフォントが適用されない。
- 以上より採用せず、`.ptl-footer` 以下に限定したセレクタ設計をやり直す方針とする。

### 今後の安全な進め方（提案）

1. **CSS読込の整理**  
   - Google Fonts は `functions.php` の既存 enqueue に統合し、`child_style` 依存を保ったまま読み込む。CSS内 `@import` は使用しない。

2. **`.ptl-footer` 配下のみスタイルする**  
   - セレクタは `html body #footer .ptl-footer …` のように `.ptl-footer` を起点に限定する。`#footer ul` 等の共通部位は触らない。

3. **レイアウトは flex/grid で再構築**  
   - absolute/float の併用は避け、既存 HTML を活かした flex（もしくは grid）で Asti 風の位置関係を再現する。SP は @media 内で `flex-direction: column` に切り替える。

4. **SP CSS を専用ファイルで維持**  
   - Phase② 以降の管理方針どおり、`css/footer.css`（PC/共通）と `css/sp/footer-sp.css`（767px以下）に分け、functions から両方を読み込む。

5. **影響範囲を段階的に確認**  
   - まずロゴ/SALON ブロックのみスタイルし、次にナビゲーション、最後にコピーライトと段階的に反映。各段階で PC/SP とも表示を確認する。

## Phase② 完了 + 重大インシデント発生と復旧

### ✅ Phase② 完了（!important除去作業）

#### 実施内容
1. **!important除去作業完了**
   - `style.css` から38箇所の `!important` を削除
   - 詳細度を上げてスタイルを維持
   - コミット: `337ccd2` - Phase② CSS optimization complete

2. **Git履歴**
   - `087713e` (HEAD) - Force GitHub cache refresh: Phase2 important removal verified complete
   - `337ccd2` - Phase② CSS optimization complete: Removed all !important declarations
   - `ba6da3b` - Phase②（部分完了）: !important除去作業

#### Phase②完了時点の状態
- **日時:** 2025年10月15日
- **コミット:** `087713e`
- **状態:** 全セクションのCSS正常、サイト表示正常

---

### 🚨 **重大インシデント: 親テーマ削除事故**

#### 発生日時
2025年10月18日 18:43頃

#### 事象
- SWELL親テーマ（`swell`）が完全に削除される
- サイト全体のUI崩壊（ヒーロー非表示、レイアウト崩壊）
- 子テーマのみが残存

#### 原因
**rsyncコマンドの誤使用:**

```bash
# 誤ったコマンド（実行してしまった）
rsync -avz --delete -e ssh swell_child/ \
  patolaqshe@www3521.sakura.ne.jp:/home/patolaqshe/www/media/wp-content/themes/

# 問題点:
# - 送信元: swell_child/（子テーマのみ）
# - 送信先: themes/（themesディレクトリ全体）
# - --delete オプション: 送信元に存在しないファイル（=親テーマswell）を削除
# → 結果: 親テーマswellが完全削除
```

**正しいコマンド:**
```bash
# 送信先も swell_child を明示する必要があった
rsync -avz --delete -e ssh swell_child/ \
  patolaqshe@www3521.sakura.ne.jp:/home/patolaqshe/www/media/wp-content/themes/swell_child/
```

#### 復旧作業
1. **親テーマの復元**
   ```bash
   # updraftバックアップから復元
   ssh patolaqshe@www3521.sakura.ne.jp 'cd /home/patolaqshe/www/media/wp-content && \
     unzip -o updraft/backup_2025-10-18-1520_patolaqshecom_media_0ee8ac487b3a-themes.zip \
     "themes/swell/*"'
   ```

2. **復元確認**
   - 親テーマ: `/home/patolaqshe/www/media/wp-content/themes/swell/`
   - ファイル数: 749ファイル
   - バージョン: SWELL 2.15.0
   - WordPress基本ファイル: 全て正常

3. **復旧完了**
   - 親テーマ復元完了
   - サイト表示正常化

---

### ⚠️ **今後の絶対厳守事項**

#### 1. rsyncコマンド使用時のルール

**❌ 絶対にやってはいけないこと:**
```bash
# themes ディレクトリ全体に --delete を使用
rsync -avz --delete ... /themes/
```

**✅ 正しい方法:**
```bash
# 必ず swell_child ディレクトリを明示
rsync -avz -e ssh --exclude ".DS_Store" --exclude "*.bak" \
  swell_child/ \
  patolaqshe@www3521.sakura.ne.jp:/home/patolaqshe/www/media/wp-content/themes/swell_child/
```

#### 2. --deleteオプションの使用禁止
- `rsync --delete` は**原則使用しない**
- やむを得ず使用する場合は、送信元と送信先のパスを3回確認
- 親テーマ`swell`には一切触れない

#### 3. ファイル削除前の確認
- `rm -rf` コマンドを実行する前に、対象ディレクトリを必ず確認
- 親テーマが含まれていないか確認
- バックアップが存在するか確認

#### 4. サーバー構造の理解
```
/home/patolaqshe/www/media/
├── wp-config.php
├── wp-load.php
└── wp-content/
    ├── themes/
    │   ├── swell/          ← 親テーマ（絶対に削除禁止）
    │   └── swell_child/    ← 子テーマ（作業対象）
    ├── plugins/
    ├── uploads/
    └── updraft/            ← バックアップ
```

---

### 📌 現在のプロジェクト状態（2025年10月18日 19:00）

#### Git状態
- **コミット:** `087713e` (Phase②終了時点にロールバック)
- **ブランチ:** `main`
- **状態:** Phase② 完了、サイト正常動作

#### サーバー状態
- **親テーマ:** 復元完了（749ファイル）
- **子テーマ:** 正常（Phase②完了状態）
- **WordPress:** 全ファイル正常
- **サイト表示:** 正常

#### 作業ファイル
- `swell_child/css/` - 全CSSファイル正常
- `swell_child/functions.php` - 正常
- `swell_child/template-parts/` - 全テンプレート正常

---

### 🔄 次のステップ

## 2025年11月6日 追記: SPガラスデザイン調整

- **確認:** `section-salon-sp.css` と `style.css`（SPメディアクエリ内）でガラスカード／透明レイヤー指定を再点検し、必要な `!important` が既に不要な状態でグローバルデフォルトと競合しないよう調整
- **デザイン:** ロゴやイントロの `drop-shadow` フィルターと SALON セクションのガラスカードを SP 専用スタイルに再導入し、視認性と統一感を確保
- **フッター:** `.p-fixBtnWrap` を直接ターゲットにして固定メニューを維持しつつ、SP フッター全体は透明背景でヘッダ／フッター以外とのバランスを維持
- **運用:** SP メディアクエリ内に `glass` スタイルを集約（`style.css`）して上書きの効率化とデスクトップ影響の回避を両立させ、コミット／同期済み

#### Phase③以降の作業
Phase②完了により、以下の作業が可能になります：
1. PC/SP CSS分離（Phase③）
2. セクション個別最適化
3. パフォーマンス改善

**ただし、今回のインシデントを踏まえ:**
- rsyncコマンドは慎重に実行
- --deleteオプションは使用しない
- 親テーマには一切触れない

---

### 📝 教訓

1. **rsync --delete は非常に危険**
   - 送信先に存在するが送信元に存在しないファイルは全て削除される
   - themesディレクトリ全体を対象にすると、親テーマも削除される

2. **バックアップの重要性**
   - updraftバックアップがあったため復旧可能だった
   - 定期的なバックアップは必須

3. **コマンド実行前の確認**
   - パスの確認（送信元/送信先）
   - オプションの確認（--deleteの有無）
   - 影響範囲の確認（親テーマが含まれないか）

---

**作成日時:** 2025年10月18日 19:00  
**最終コミット:** `087713e` - Force GitHub cache refresh: Phase2 important removal verified complete  
**インシデント:** 親テーマ削除 → 復旧完了
